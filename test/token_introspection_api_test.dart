// import 'package:kkbox_openapi/kkbox_auth.dart';
// import 'package:test/test.dart';
// import 'client_setup.dart';

void main() {
//   group('Test Track API', () {
//     ClientConfiguration config;
//     String accessToken;

//     setUp(() async {
//       config =
//           ClientConfiguration(clientId: clientID, clientSecret: clientSecret);
//       final flow = ClientCredentialsFlowApi(configuration: config);
//       final result = await flow.run();
//       accessToken = result.accessToken;
//     });

//     test('First Test', () async {
//       final api = TokenIntrospectionApi(
//           accessToken: accessToken, configuration: config);
//       final introspection = await api.run();
//       expect(introspection.issuer.isEmpty, isFalse);
//       expect(introspection.expiryDate != null, isTrue);
//       expect(introspection.issueDate != null, isTrue);
//       expect(introspection.clientId.isEmpty, isFalse);
//     });
//   });
}
